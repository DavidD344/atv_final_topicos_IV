[project]
name = "spl-nfp-sampling"
version = "0.1.0"
description = "Experiments comparing sampling strategies for NFP prediction in software product lines."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.9",
    "numpy>=2.0",
    "pandas>=2.2",
    "scikit-learn>=1.5",
]

[tool.uv]
package = false
